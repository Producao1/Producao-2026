{
  "name": "vanta-store",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p public .vercel/output/static && cp index.html public/ && cp styles.css public/ && cp script.js public/ && cp -R public/. .vercel/output/static/",
    "start": "python3 -m http.server 3000"
  }
}
